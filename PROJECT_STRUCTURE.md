# NEET Project Structure

This workspace is organized into separate frontend and backend folders for easy management.

## Top-Level Layout

- `frontend/flutter_application_1/` -> Flutter mobile app
- `backend/` -> Python backend (FastAPI/services/docs)

## Notes

- Backend was moved from `flutter_application_1/backend` to `backend/`.
- Frontend app has been copied to `frontend/flutter_application_1/`.
- The old root folder `flutter_application_1/` still exists because some files are currently locked by another running process.

## Recommended Active Paths

- Frontend work: `frontend/flutter_application_1/`
- Backend work: `backend/`
