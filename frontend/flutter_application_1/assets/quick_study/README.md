Place subject images here. Expected filenames (lowercase, spaces->underscores):
- physics.png
- chemistry.png
- biology.png

Optional extras:
- zoology.png
- botany.png

Images should be roughly 400x300 or larger. The app falls back to an icon if an image is missing.
